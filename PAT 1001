#include<stdio.h>
int main()
{  int n,j,k=0;
    scanf("%d",&n);
  for( ;n!=1 ;k++ ){
      if(n%2==0){j=1;}
      if(n%2==1){j=2;}
      switch(j){
          case 1:n/=2;
          break;
          case 2:n=3*n+1;n/=2;
          break;
      }
  }
  printf("%d",k);   
}
